#!/bin/bash


# --cpus-per-task=32 --ntasks=1 --mem=128000mb --tasks-per-node=1 --partition=gpu --gres=gpu:1 --job-name=tf_random_classifier --time=10-48:00:00
# hetjob
# --cpus-per-task=32 --ntasks=1 --mem=128000mb --tasks-per-node=1 --partition=gpu --gres=gpu:1

#echo "Het size:"
#echo "$SLURM_HET_SIZE"

# sleep 20

# Getting the node names
nodes=$(scontrol show hostnames "$SLURM_JOB_NODELIST")
nodes_array=($nodes)
echo "Node list: $nodes"
echo "Node array: $nodes_array"
echo "Num nodes: $SLURM_JOB_NUM_NODES"

srun --het-group=0 "sleep 10"
srun --het-group=1 "sleep 10"
